import { TChildren } from "@/types/someShortTypes";

const HomeMarginContainer = ({ children }: TChildren) => {
  return <div className="mt-12">{children}</div>;
};

export default HomeMarginContainer;
